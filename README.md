# BFS Pathfinding Visualizer

This project is a visualizer for the **Breadth-First Search (BFS)** algorithm on a 15x15 grid.  
It features a **frontend-backend architecture**: the frontend is built with **React + TypeScript + TailwindCSS**, and the backend is implemented with **FastAPI** to provide the BFS algorithm service.
